version: "3.8"

services:
  backend:
    build: .
    container_name: flora-be
    restart: unless-stopped
    ports:
      - "8000:8000"
    env_file:
      - .env
    networks:
      - flora-network

networks:
  flora-network:
    driver: bridge
